{
  "name": "@enduragent/sport-cycling",
  "version": "0.0.6",
  "private": true,
  "description": "Cycling sport package — soul, skills, tools, schemas, must-preserve tokens. Implements the Sport contract from @enduragent/core.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./migrate": "./dist/migrate.js",
    "./workout-import": "./dist/workout-import/index.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "npm run codegen && tsup",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "codegen": "node --import tsx ../../tools/generate-sport-skills.ts"
  },
  "dependencies": {
    "@enduragent/engine": "workspace:*",
    "@enduragent/kernel": "workspace:*",
    "intervals-icu-api": "^0.4.0",
    "ai": "^6.0.158",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.4.0",
    "tsx": "^4.23.13",
    "typescript": "^6.0.2",
    "vitest": "^4.1.11"
  }
}
