{
  "name": "@enduragent/coach-contract",
  "version": "0.1.2",
  "private": true,
  "description": "Engine/UI contract seam: CoachEngine interface, request/response schemas, TurnEvent union, AthleteState, protocol version, exit codes.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsup && tsc --emitDeclarationOnly",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.4.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.11"
  }
}
