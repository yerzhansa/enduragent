{
  "name": "@enduragent/sport-running",
  "version": "0.1.3",
  "private": true,
  "description": "Running sport package — critical-speed-anchored pace zones, soul, skills, tools. Implements the Sport contract from @enduragent/core.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "npm run codegen && tsup",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "codegen": "node --import tsx ../../tools/generate-sport-skills.ts"
  },
  "dependencies": {
    "@enduragent/engine": "workspace:*",
    "@enduragent/kernel": "workspace:*",
    "intervals-icu-api": "^0.4.0",
    "ai": "^6.0.158",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.4.0",
    "tsx": "^4.23.12",
    "typescript": "^6.0.2",
    "vitest": "^4.1.11"
  }
}
