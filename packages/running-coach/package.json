{
  "name": "running-coach",
  "version": "0.0.8",
  "private": true,
  "description": "AI running coaching agent — BYOK + intervals.icu + Telegram.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "running-coach": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@enduragent/core": "workspace:*",
    "@enduragent/sport-running": "workspace:*"
  },
  "devDependencies": {
    "@enduragent/engine": "workspace:*",
    "@types/node": "^25.6.0",
    "tsup": "^8.4.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.11"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22"
  }
}
