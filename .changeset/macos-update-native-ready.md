---
"@enduragent/desktop": patch
---

User-facing: On a Mac, Restart appears only after an update is ready to install. If preparation fails, the app stays open so you can try again. Install this version once by hand if the app you have now cannot update itself.
