---
---

Initialize the Apple server library within a request and use the Workers HTTP transport so the credits worker can start and verify Apple notifications in Cloudflare. Test the bundled worker in the Workers runtime.
