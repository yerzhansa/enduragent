---
"@enduragent/engine": patch
"@enduragent/sport-cycling": patch
"@enduragent/desktop": patch
"cycling-coach": patch
---

User-facing: The coach reads a shorter set of instructions on every message, so each reply costs a little less. Power is now called weighted avg power everywhere, and the coach no longer rejects realistic FTP values.
