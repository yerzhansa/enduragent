---
"@enduragent/engine": patch
---

Add an opt-in engine generation setting to evaluate caching of tool results during multi-step calls. Keep the default cache behavior unchanged until full-task cost measurements justify enabling it.
