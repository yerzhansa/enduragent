# Production-chart verification maintenance

Status: **blocked**. Proven map corrections are complete; complete live coverage and one cleanup step remain blocked.

The only maintained target is `verify-npm-coach`, inspected at `0e9f8aff`. No product source, product tests, executor, or runner behavior changed. No PR was created and no evidence was uploaded.

## Corrections

All 17 feature pages received a separate read-only source investigation. The map retains all 175 inventory entries and 51 approval criteria. It now records the current revision and 240 concrete source/test paths. Added the existing chart model, renderer, preparation and regression files to their owning feature pages. Automatic chart cards, structured edit conversion, pending rename preservation, localized N of NN positions and complete delivery are production checks, not prototype claims.

Scoped fixture Telegram restart limits to that executor and retained the separately owned real runtime route. Corrected synthetic-only wording without replacing its executor. Corrected completion prose against accepted B12–B13: this feature excludes physical completion detection; applied proposal actions must not replay. Clarified cold-cache resend's independent prose and durable-review sources. The release recipe now compares the response with the selected npm package version.

See [source-audit.json](source-audit.json) for every feature's cited entry points, existing recipe, component files and remaining limitations.

## Verification

Map consistency and doctor passed. All 12 existing terminal routes passed, including pending restart and the three coaching fixtures. Each reported no live owned PID, scratch removed and zero listeners. Setup cancellation reached DeepSeek, Other, fictional-model and the endpoint prompt, then exited without saving configuration or authentication.

The assigned component run passed 2,806 tests and skipped one existing integration test; one large-batch test exceeded its five-second timeout. That unchanged test passed alone in 1.93 seconds. The two newly mapped existing chart suites passed 13 tests. Combined evidence covers 2,820 passes and one skipped integration test across 124 mapped files. The initial timeout remains retained, not hidden.

Real Telegram reused the existing guarded OpenRouter DeepSeek runtime. Version delivery, language override disclosure, snapshot help and empty feedback usage were observed. Release-note retrieval displayed its unavailable fallback. No calendar mutations, new model inference, software updates, preference changes or feedback submissions were requested. These checks do not re-run the earlier chart mutation campaign; its revision-specific evidence remains linked from the chart checks.

## Product issue

Telegram `/version` reports `2026.5.9`; the selected built npm package declares `2026.9.17`. Direct Node resolution from the bundle location finds an installed cycling-coach package at `2026.5.9`. The lookup in `packages/core/src/updater.ts:87–111` explains that metadata choice. Record version correctness as FAIL. No product correction belongs in this maintenance pass.

## Remaining coverage and cleanup

The existing runners do not provide every controlled outage, delivery-failure, first-pairing, effective locale-switching or persistent fictional-Telegram restart route. Hosting, external authentication/vault lifecycle and connected-device checks need their corresponding isolated platforms/accounts/devices. These remain BLOCKED or NOT RUN, not verified-unreachable and not passed by component tests. Sender administration was not re-driven because its proposed run/cleanup command was blocked before execution; historical proof retains its original scope.

The twelve terminal runners cleaned up. No new poller, browser profile or tab was created; the existing real bot remains with its owner. The setup PTY exited, but the host destructive-command guard rejected removal of its one owned scratch directory and required explicit permission. Evidence was retained in [setup-result.json](setup-result.json); the human cleanup question remains pending. Do not bypass the guard or claim full cleanup.

See [summary.json](summary.json) and [Telegram observations](telegram-observations.json). Raw temporary evidence is preserved. Retained fictional transcripts replace private paths and shift current-era training-date examples to 1998; source revision and binary hash remain factual.

Final map and doctor checks passed. Repository privacy checked 4,086 source files and 21 golden fixtures; the separate retained-evidence scan found no token-shaped values, real athlete IDs, private paths or current-era fictional training dates. Product worktree remains clean.
